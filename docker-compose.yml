version: '3.8'

services:
  securenode:
    image: vankir/securenode:latest
    container_name: securenode
    restart: unless-stopped
    ports:
      - "5005:5005"
    deploy:
      resources:
        limits:
          memory: 1G